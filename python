def MissingNumbers(x):
  numbers = allnumbers(x)
  length = len(x)
  output = []
  for i in range(1, x[-1])
    if i not in numbers:
      output.append(i)
  return output
  
list  = [1, 3, 5,6,7,8,10]
print(MissingNumbers(list))
